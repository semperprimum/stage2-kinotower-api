import React from 'react';

const Footer = ({className}) => {
    return (
        <footer className={"w-full h-16 bg-slate-300 mt-0 flex justify-center items-center " + className}>
            COPYRIGHT
        </footer>
    );
};

export default Footer;